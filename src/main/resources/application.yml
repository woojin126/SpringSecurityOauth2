spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/security?serverTimezone=Asia/Seoul
    username: cos
    password: cos1234

  devtools:
    livereload:
      enabled: true

  freemarker:
    cache: false

  jpa:
    hibernate:
      ddl-auto: update   #애플리케이션  실행시점에 내가가지고잇는 엔티티를지우고(테이블) 다시생성
    properties:
      hibernate:
        #show_sql: true 엔 sout에 출력해서  안쓰는게좋다
        format_sql: true
        default_batch_fetch_size: 100

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 251592878751-hde0mp1rhrduev9r6i822osm1r9bi88g.apps.googleusercontent.com
            client-secret: PcBIXfjuKmR1fRpH6GRCy1V7
            scope:
            - email
            - profile


          facebook:
            client-id: 259294199111035
            client-secret: 26f0055f04462d961d2231e7394edfbf
            scope:
              - email
              - public_profile

            #redirect-uri: http://localhost:8080/login/oauth2/code/facebook
          naver: # 기본적으로 Oauth2-Client 라이브러리가 들고있지않은 라이브러리라 위와같이하면 에러발생 Provider ID must be specified for client registration 'naver'
            client-id: C9fU7GyZ7vMFwMhF0mNw
            client-secret: RQvOO4fNdI
            scope:
              - name
              - email
            client-name: Naver
            authorization-grant-type: authorization_code #4가지 방식중 우리는 Authorization Code Grant Type 방식
            redirect-uri: http://localhost:8080/login/oauth2/code/naver #구글이나 페이스북은 자동설정되어있어서 안써도됨


        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize # 네아로 인증요청
            token-uri: https://nid.naver.com/oauth2.0/token # 접근토큰 발급 요청
            user-info-uri: https://openapi.naver.com/v1/nid/me # 프로필 정보 조회
            user-name-attribute: response # 회원정보를 Json으로 받는데 Response라는 키값으로 네이버가 리턴해줌



logging:
  level:
    org.hibernate.SQL: debug
    # org.hibernate.type: trace


